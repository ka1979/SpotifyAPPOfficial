import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#181823', 
    },
    secondary: {
      main: '#537FE7', 
    },
    tertiary: {
      main: '#C0EEF2', 
    },
    quaternary: {
      main: '#E9F8F9', 
    },
  },
});
